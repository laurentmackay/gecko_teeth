    function y=sense(theta)
        y=((1-sin(theta))/2);
        % y=((1-sin(theta+pi/2))/2);
        % y=-(((1-sin(theta))/2)-sin((pi/4)+theta/2).^6);
        % s=sin(theta);
        % eps2=0.1;
        %  y=((1-sqrt(1+eps2)*s./sqrt(s.*s+eps2))/2);
    end
